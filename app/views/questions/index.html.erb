 Questions:
 <br>
 <% if !@questions.empty? %>
  <% @questions.each do |q| %>
      <%= link_to q.title, q %><br>
      answers:
      <%= q.answers.count %> <br>
  <% end %>
<% end %>
<%= link_to 'Ask question', new_question_path %>
